{
  "compilerOptions": {
    /* Lenguaje y Entorno */
    "target": "ES2020", // Usa características modernas de JavaScript (ES2020)
    "lib": ["DOM", "DOM.Iterable", "ESNext"], // Incluye tipos para el navegador y ESNext
    "jsx": "react-jsx", // Usa la transformación de JSX de React 17+

    /* Módulos */
    "module": "ESNext", // Usa módulos ESM (import/export)
    "moduleResolution": "Node", // Resuelve módulos como Node.js
    "resolveJsonModule": true, // Permite importar archivos JSON
    "isolatedModules": true, // Asegura que cada archivo sea un módulo independiente

    /* Emit */
    "noEmit": true, // No genera archivos .js (Vite se encarga de la compilación)

    /* Interoperabilidad */
    "esModuleInterop": true, // Permite importar módulos CommonJS como ESM
    "allowSyntheticDefaultImports": true, // Permite importaciones sintéticas de default

    /* Verificación de Tipos */
    "strict": true, // Habilita todas las opciones de verificación estricta
    "forceConsistentCasingInFileNames": true, // Asegura consistencia en mayúsculas/minúsculas
    "skipLibCheck": true, // Omite la verificación de tipos en archivos .d.ts

    /* Rutas y Alias */
    "baseUrl": ".", // Define la ruta base para los alias
    "paths": {
      "@/*": ["./src/*"] // Configura el alias @/ para apuntar a la carpeta src
    },

    /* Otras Opciones Útiles */
    "allowJs": false, // No permite archivos JavaScript (solo TypeScript)
    "noImplicitAny": true, // Reporta errores cuando se infiere el tipo 'any'
    "strictNullChecks": true, // Habilita la verificación estricta de null/undefined
    "strictFunctionTypes": true, // Verifica tipos en funciones de manera estricta
    "strictBindCallApply": true, // Verifica los argumentos de bind, call y apply
    "strictPropertyInitialization": true, // Asegura que las propiedades de clase estén inicializadas
    "noUnusedLocals": true, // Reporta variables locales no utilizadas
    "noUnusedParameters": true, // Reporta parámetros no utilizados
    "noImplicitReturns": true, // Reporta funciones que no retornan explícitamente
    "noFallthroughCasesInSwitch": true // Reporta casos fallthrough en switch
  },
  "include": ["src/**/*"], // Incluye todos los archivos en la carpeta src
  "exclude": ["node_modules", "dist"] // Excluye estas carpetas de la compilación
}
